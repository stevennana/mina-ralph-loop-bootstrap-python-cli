[project]
name = "mina-ralph-loop-bootstrap-python-cli"
version = "0.1.0"
description = "Codex skill for bootstrapping Python CLI repos around a Ralph loop."
requires-python = ">=3.12"

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
